import 'package:flutter/material.dart';

class WidgetStockEntry extends StatelessWidget {
  const WidgetStockEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}